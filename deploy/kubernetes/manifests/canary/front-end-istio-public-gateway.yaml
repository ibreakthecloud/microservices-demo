apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: public-gateway-front-end
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 30002
        name: http
        protocol: HTTP
      hosts:
        - "*"